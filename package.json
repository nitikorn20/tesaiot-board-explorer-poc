{
  "name": "tesaiot-board-explorer-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Independent static design PoC for a TESAIoT board showcase and selected hardware example catalog",
  "scripts": {
    "dev": "node serve.mjs",
    "sync": "node scripts/sync-catalog.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=22" },
  "license": "Apache-2.0"
}
