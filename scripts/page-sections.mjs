import { icon } from '../dist/icons.js';
import { escapeHTML as e } from '../dist/catalog.js';
const tx=(th,en,tag='span',attrs='')=>`<${tag} data-th="${e(th)}" data-en="${e(en)}" ${attrs}>${e(th)}</${tag}>`;
const a=(href,th,en,cls='text-link')=>`<a class="${cls}" href="${href}" ${href.startsWith('https:')?'target="_blank" rel="noopener noreferrer"':''}>${tx(th,en)}${icon('arrow')}</a>`;
const sectionHead=(eyebrow,th,en,detailTh,detailEn)=>`<div class="section-heading"><div><p class="eyebrow">${eyebrow}</p>${tx(th,en,'h2')}</div>${tx(detailTh,detailEn,'p')}</div>`;
const photo=(kind,cls='')=>kind==='training'?`<img class="${cls}" src="assets/training-board.webp" alt="QWA309 + KIT_PSE84_AI Training assembly" width="1000" height="750">`:`<svg class="ndr-photo ${cls}" viewBox="180 520 410 280" role="img" aria-label="TSOM on a Luckfox reference carrier; conceptual composition, not a finished NDR board"><image href="assets/ndr-reference-concept.png" x="0" y="0" width="773" height="800"/></svg>`;

const home=`
<section class="hero wrap" id="hardware"><div class="hero-copy"><p class="eyebrow"><span class="line"></span> YOUR IDEAS. REAL HARDWARE.</p><h1>${tx('จากไอเดีย','From first signal.')}<br>${tx('สู่สิ่งที่จับต้องได้','To your next idea.','em')}</h1>${tx('หมุน สัมผัส ฟัง และเชื่อมต่อ — สำรวจบอร์ดผ่านสิ่งที่คุณสร้างได้ แล้วเริ่มต่อยอดจาก Example ที่ตรงกับ Hardware','Turn, touch, listen, connect. Discover what you can build, explore the hardware behind it, then make an example your own.','p','class="lead"')}<div class="actions">${a('#boards','เลือกบอร์ดของคุณ','Find your board','button primary')}${a('examples.html','สำรวจ Examples','Explore examples','button secondary')}</div><div class="hero-facts"><div><b>02</b>${tx('เส้นทางการพัฒนา','board paths')}</div><div><b>09</b>${tx('Examples ที่คัดมา','curated examples')}</div><div><b>&lt;/&gt;</b>${tx('ต่อยอดจาก Source','source-led learning')}</div></div><p class="hero-footnote">${icon('check')}${tx('เริ่มจากของจริง · เห็น Requirements ก่อนทดลอง','Start with real hardware. Check requirements before you build.')}</p></div>
<div class="tour-shell"><div class="tour-toolbar"><div class="board-switch" role="group" aria-label="Explore a board"><button type="button" data-board="training" aria-pressed="true"><span>01</span> QWA309</button><button type="button" data-board="ndr" aria-pressed="false"><span>02</span> TSOM / NDR</button></div><span class="live-label">INTERACTIVE TOUR</span></div><div id="board-stage" class="board-stage"></div><div class="tour-bottom"><p id="board-caption"></p><div class="tour-step"><button id="hotspot-prev" type="button" aria-label="Previous hardware point">←</button><span id="hotspot-count">01 / 08</span><button id="hotspot-next" type="button" aria-label="Next hardware point">→</button></div></div><div id="hotspot-detail" class="hotspot-detail" aria-live="polite"></div></div></section>
<div class="capability-rail wrap" aria-label="Capabilities"><span>${icon('sliders')} INPUT</span><span>${icon('display')} DISPLAY</span><span>${icon('sensor')} SENSING</span><span>${icon('audio')} AUDIO</span><span>${icon('network')} CONNECTIVITY</span></div>
<section class="section wrap" id="boards">${sectionHead('01 / CHOOSE YOUR STARTING POINT','สองบอร์ด คนละจุดเริ่มต้น','Two boards. Two starting points.','เริ่มจากชุดฝึกที่มีตัวอย่าง หรือติดตามแพลตฟอร์ม SOM + Base รุ่นใหม่','Start with a kit and source examples, or explore the new SOM + base-board concept.')}<div class="board-lineup"><article class="lineup-card training-card reveal"><div class="lineup-header"><span class="mono">01 / TRAINING SERIES</span><span class="status">${tx('มี SDK และ Source','SDK & source available')}</span></div><div class="lineup-photo">${photo('training')}</div><div class="lineup-copy"><h3>QWA309 Training Kit</h3><p class="model">QWA309 + KIT_PSE84_AI</p>${tx('พื้นที่เริ่มต้นสำหรับลอง Input, Sensor และ HMI เห็นผลลัพธ์บน Hardware แล้วค่อยรวมเป็น Application','A hands-on starting point for inputs, sensors and HMIs. See results on hardware, then combine them into an application.','p')}<div class="feature-chips"><span>${icon('sliders')} Pot ×4</span><span>${icon('display')} Touch LCD</span><span>${icon('grid')} RGB Matrix</span></div><button type="button" class="text-link" data-explore-board="training">${tx('สำรวจ Hardware 8 จุด','Explore 8 hardware points')}${icon('arrow')}</button></div></article><article class="lineup-card ndr-card reveal"><div class="lineup-header"><span class="mono">02 / NEXT BOARD</span><span class="status draft">${tx('แบบร่าง / รอยืนยัน','Draft / pending')}</span></div><div class="lineup-photo reference-photo">${photo('ndr')}<span class="reference-badge">REFERENCE CONCEPT</span></div><div class="lineup-copy"><h3>TSOM + NDR Base</h3><p class="model">MODULAR PLATFORM / DEVELOPMENT PREVIEW</p>${tx('สำรวจแนวคิดแยกส่วนประมวลผล SOM และ Base สำหรับต่อยอด Interface โดยยืนยัน Revision และ BSP ก่อนนำไปใช้','Explore a modular compute-and-carrier concept. Confirm the base revision, interfaces and BSP before building on it.','p')}<div class="feature-chips"><span>${icon('chip')} TSOM</span><span>${icon('usb')} USB*</span><span>${icon('audio')} Audio*</span></div><button type="button" class="text-link" data-explore-board="ndr">${tx('สำรวจแนวคิดและสถาปัตยกรรม','Explore concept & architecture')}${icon('arrow')}</button><p class="fineprint">${tx('* ตามร่างหน้า 10 ภาพใช้ Luckfox เป็น Reference ไม่ใช่ PCB NDR ฉบับสุดท้าย','* Based on slide 10. The image uses a Luckfox reference, not the final NDR PCB.')}</p></div></article></div></section>
<section class="architecture-section" id="architecture"><div class="wrap">${sectionHead('02 / UNDERSTAND THE HARDWARE','เห็นภาพรวม แล้วเลือกจุดต่อยอด','See the system. Find your starting point.','Block Diagram วาดด้วย HTML/SVG กดแต่ละส่วนเพื่ออ่านหน้าที่และข้อจำกัด','An HTML/SVG functional diagram. Select a block to explore its role and constraints.')}<div class="architecture-heading"><h3 id="architecture-title">QWA309 Training Kit</h3><div class="board-switch" role="group" aria-label="Architecture board"><button data-board="training" aria-pressed="true">QWA309</button><button data-board="ndr" aria-pressed="false">TSOM / NDR</button></div></div><div id="architecture-content"></div><div id="architecture-detail" class="architecture-detail" aria-live="polite"></div>${tx('Functional overview ไม่ใช่ Schematic หรือ Pin Map · เส้นแสดงการเชื่อมต่อเชิงหน้าที่ ไม่ใช่สัญญาณที่กำลังวิ่งจริง','Functional overview, not a schematic or pin map. Lines show functional connections, not live signals.','p','class="fineprint"')}</div></section>
<section class="section wrap playground-section" id="playground"><div class="playground-copy"><p class="eyebrow">03 / A SMALL INPUT. A VISIBLE RESULT.</p>${tx('ลองหมุนไอเดียให้เป็นสี','Turn an idea into color.','h2')}${tx('หนึ่ง Example ที่อธิบายบอร์ดได้ดีกว่ารายการสเปก: Pot 3 ช่อง เปิด–ปิด R/G/B แล้วผสมเป็น 8 สี','One example can explain more than a specification list: three potentiometers switch R/G/B channels to create eight colors.','p')}<p class="simulation-label">${icon('code')}${tx('Browser simulation · ไม่เชื่อมต่อบอร์ด','Browser simulation · No board connected')}</p>${a('examples.html#rgb','ดู Example บน Hardware','See the hardware example')}</div><div class="playground"><div class="playground-top"><span class="mono">POT → THRESHOLD → RGB MATRIX</span><span id="color-code">RGB 101</span></div><div class="playground-inner"><div class="pot-controls">${['R','G','B'].map((channel,i)=>`<label class="pot-control"><span><b>${channel}</b><output id="pot-${channel}-value">${[70,20,80][i]}%</output></span><input id="pot-${channel}" type="range" min="0" max="100" value="${[70,20,80][i]}" aria-label="${channel} potentiometer simulation"></label>`).join('')}</div><div class="matrix" id="rgb-matrix" role="img" aria-label="Simulated RGB matrix">${Array.from({length:64},()=>'<i></i>').join('')}</div></div><p>${tx('มากกว่า 50% = เปิดช่องสีนั้น · ไม่ใช่การหรี่สีต่อเนื่อง','Above 50% turns a channel on. This is not continuous brightness control.')}</p></div></section>
<section class="section wrap examples-section">${sectionHead('04 / MAKE SOMETHING YOURS','เลือกสิ่งที่อยากสร้าง','What will you build first?','เริ่มจากตัวอย่างที่อธิบายอุปกรณ์และมี Source ให้ต่อยอด','Start with clear hardware requirements and source you can explore.')}<div id="featured-grid" class="example-grid"><p>${tx('กำลังโหลด Examples…','Loading examples…')}</p></div><div class="section-bottom">${a('examples.html','ดูทั้ง 9 Examples','Explore all 9 examples','button secondary')}</div></section>
<section class="start-section" id="start"><div class="wrap">${sectionHead('YOUR NEXT STEP','จากหน้าจอนี้ สู่โปรเจกต์ของคุณ','From this page to your project.','เลือกเส้นทางที่ตรงกับบอร์ด ไม่ต้องเริ่มจากศูนย์','Choose a board-matched path. You do not have to start from scratch.')}<div class="journey"><article><b>01</b>${tx('เลือกบอร์ด','Pick your board','h3')}${tx('ตรวจรุ่นและอุปกรณ์ที่มี','Check the board revision and available hardware.','p')}${a('#boards','ดูบอร์ด','See boards')}</article><article><b>02</b>${tx('เลือก Example','Find an example','h3')}${tx('เปิด Requirements และ README','Open its requirements and README.','p')}${a('examples.html','ค้นหาตัวอย่าง','Browse examples')}</article><article><b>03</b>${tx('ลงมือพัฒนา','Start building','h3')}${tx('ตั้งค่า SDK ตามเอกสารต้นทาง','Set up the SDK using the original documentation.','p')}${a('https://tesaiot.github.io/tesaiot-pse84-devkit-sdk/','เปิด SDK & Docs','Open SDK & docs')}</article></div><p class="release-note">${icon('shield')}${tx('Online Flash ยังไม่เชื่อมต่อใน PoC นี้ · NDR Examples จะเพิ่มหลังยืนยัน Revision, BSP และ Validation','Online Flash is not connected in this PoC. NDR examples follow confirmed revision, BSP and validation.')}</p></div></section>`;

const categories=[['all','ทั้งหมด','All examples'],['control','Input & Control','Input & Control'],['sensors','Sensors','Sensors'],['audio','Audio','Audio'],['dashboard','Dashboard','Dashboard'],['connectivity','Connectivity','Connectivity']];
const option=(value,th,en)=>tx(th,en,'option',`value="${value}"`);
const examples=`<section class="catalog-hero wrap"><div><p class="eyebrow">THE EXAMPLE COLLECTION</p><h1>${tx('ไอเดียที่ใช่','Find your spark.')}<br>${tx('เริ่มลองได้จากที่นี่','Build from here.','em')}</h1>${tx('เลือกสิ่งที่สนใจ ดูอุปกรณ์ที่ต้องใช้ แล้วเปิด Source ไปต่อ','Choose an idea, check the hardware, and follow the source.','p','class="lead"')}</div><div class="catalog-intro"><span class="catalog-number">09<span> / SELECTED</span></span>${tx('คัดเฉพาะ Hardware Examples พร้อมภาพจากต้นทางและ Requirements','A hardware-focused collection with source images and clear requirements.','p')}${a('sources.html','ดูแหล่งข้อมูล','View provenance')}</div></section>
<section class="catalog-section wrap" aria-label="Example catalog"><div class="catalog-toolbar"><div class="search-field"><label for="search">${tx('ค้นหา Example','Search examples')}</label><div>${icon('search')}<input id="search" type="search" maxlength="120" autocomplete="off" placeholder="ชื่อ Example, Sensor หรือ Feature…" data-th-placeholder="ชื่อ Example, Sensor หรือ Feature…" data-en-placeholder="Example, sensor or feature…"></div></div><div class="select-field"><label for="board-filter">${tx('บอร์ด','Board family')}</label><select id="board-filter">${option('all','ทุกบอร์ด','All boards')}${option('training','Training / PSoC Edge Kit','Training / PSoC Edge Kit')}${option('ndr','TSOM + NDR Base','TSOM + NDR Base')}</select></div><div class="select-field"><label for="level-filter">${tx('ระดับจากต้นทาง','Source difficulty')}</label><select id="level-filter">${option('all','ทุกระดับ','All levels')}${option('beginner','Beginner','Beginner')}${option('intermediate','Intermediate','Intermediate')}${option('advanced','Advanced','Advanced')}</select></div></div><div class="category-tabs" role="group" aria-label="Category">${categories.map(([value,th,en])=>`<button type="button" data-category="${value}" aria-pressed="${value==='all'}">${tx(th,en)}</button>`).join('')}</div><div class="results-heading"><p id="result-count" role="status" aria-live="polite">${tx('กำลังโหลด…','Loading…')}</p><button class="reset-link" id="reset-filters">${tx('ล้างตัวกรอง','Reset filters')} ↺</button></div>${tx('หมวดบอร์ดใช้ช่วยค้นหา ไม่ใช่คำรับรองผลทดสอบ ตรวจ Board ID และ BSP ก่อนใช้','Board families are browsing categories, not validation certificates. Check the original Board ID and BSP.','p','class="compat-note"')}<div id="catalog-grid" class="example-grid" aria-busy="true"></div><div id="catalog-message" class="empty-state" hidden></div><noscript>Please enable JavaScript for filtering, or <a href="https://github.com/tesaiot/developer-hub">visit the source repository</a>.</noscript></section><section class="flash-note wrap">${icon('usb')}<div>${tx('เลือก Example ได้แล้ว เชื่อม Flash ในขั้นถัดไป','Discover now. Flash integration comes later.','h2')}${tx('PoC นี้ไม่มีการต่อ USB, Build หรือ Flash Firmware เปิดระบบปัจจุบันได้จากลิงก์ด้านข้าง','This PoC does not connect USB, build or flash firmware. The existing service is linked separately.','p')}</div>${a('https://dev.tesaiot.dev/','Developer Hub ปัจจุบัน','Existing Developer Hub')}</section><dialog id="example-dialog" aria-labelledby="detail-title"><div class="dialog-top"><span class="mono">EXAMPLE / SOURCE PREVIEW</span><form method="dialog"><button class="icon-button" data-th-aria-label="ปิดรายละเอียด" data-en-aria-label="Close details" aria-label="ปิดรายละเอียด">×</button></form></div><div id="dialog-content"></div></dialog>`;

const sources=`<section class="sources-page wrap"><p class="eyebrow">TRANSPARENCY / SOURCES / NEXT STEPS</p><h1>Sources & scope</h1>${tx('ข้อมูลชัดเจน เพื่อเริ่มต้นได้อย่างมั่นใจ','Clear provenance. Better starting points.','p','class="lead"')}<section class="source-section">${tx('สิ่งที่ PoC นี้นำเสนอ','What this PoC provides','h2')}<div class="scope-grid"><article>${icon('board')}${tx('Board Explorer','Board explorer','h3')}${tx('ภาพบอร์ดพร้อมหมายเลขและคำอธิบาย Hover/Click/Keyboard, Block Diagram แบบ HTML/SVG และแบบจำลอง Pot → RGB ใน Browser','Numbered image hotspots for hover, click and keyboard, HTML/SVG block diagrams, and a browser-only Pot → RGB simulation.','p')}</article><article>${icon('code')}<h3>Example Catalog</h3>${tx('9 ตัวอย่าง Hardware พร้อม Search/Filter, Original Board ID, Requirements และลิงก์ README/Source ที่ล็อก Commit','Nine hardware examples with search, filters, original Board IDs, requirements and commit-pinned README/source links.','p')}</article></div>${tx('ยังไม่มี Online Flash, Firmware Build, Hardware Validation, ระบบซื้อสินค้า หรือการแก้เว็บไซต์ Production','No online flash, firmware build, hardware validation, checkout or production-site changes.','p','class="notice"')}</section>
<section class="source-section">${tx('ภาพและข้อมูลบอร์ด','Hardware images and data','h2')}<div class="scope-grid"><article><h3>QWA309 / Training</h3>${tx('ใช้ภาพจริงและตำแหน่ง Hotspot จากเว็บ SDK ต้นทาง แยก Controller KIT_PSE84_AI, QWA309 Base และโมดูลเสริม OPTIGA อย่างชัดเจน','Uses the real SDK-site product image and its hotspot positions. KIT_PSE84_AI controller, QWA309 base and optional OPTIGA module are distinguished.','p')}${a('https://github.com/tesaiot/tesaiot-pse84-devkit-sdk/tree/ef72c1b658178eee8c38b1e47d28b006f80a59b5','SDK source snapshot','SDK source snapshot')}</article><article><h3>TSOM + NDR</h3>${tx('ผู้ใช้เลือกภาพและ Diagram จาก Canva ต้นทางหน้า 10 ภาพเป็นการวาง TSOM บนฐาน Luckfox เพื่อประกอบแนวคิด ไม่ใช่ภาพ Prototype หรือ PCB NDR ที่เสร็จแล้ว เว็บไซต์แสดงเฉพาะส่วนภาพบอร์ด และวาด Diagram ใหม่ด้วยโค้ด','The user selected the image and diagram on source Canva slide 10. The image is a TSOM-on-Luckfox reference composition, not an NDR prototype or finished PCB. The site displays the board region and redraws the diagram in code.','p')}${tx('ใช้เฉพาะภาพ/แนวคิดที่เลือก ไม่เผยแพร่สไลด์ทั้งชุด เอกสารภายใน หรือสัญญา','Only the selected image/concept is used; no full slide deck, internal documents or contracts are published.','p')}</article></div>${tx('ร่าง NDR: Ethernet เป็น Optional; USB Host/Device ใช้ PHY ร่วม; Display เป็น Connector ภายนอก; USB Camera ต้องผ่านการตรวจ Host/Power/BSP/รุ่นกล้อง','NDR draft: Ethernet is optional; USB Host/Device share a PHY; the display is an external connector; USB cameras require Host, power, BSP and device checks.','p','class="notice"')}</section>
<section class="source-section">${tx('Example provenance','Example provenance','h2')}<p>Built with <a href="https://github.com/tesaiot/developer-hub">TESAIoT Platform Examples</a> · © 2025 TESAIoT Platform by TESA.</p>${tx('ข้อมูลเป็น Snapshot จาก GitHub ไม่เรียก Live API และไม่ส่ง Token ให้ผู้ชม ภาพ Screenshot 7 รูปคงไฟล์ต้นทาง ส่วน Pot RGB/ADC ใช้ภาพ Hardware อ้างอิงและติดป้ายชัดเจน','Data is a GitHub snapshot, not a live API connection. No token reaches visitors. Seven screenshots preserve the source files; Pot RGB/ADC use clearly labeled hardware-reference images.','p')}<ul><li>Episodes <code>e48fbd2a8d786</code></li><li>Practise codes <code>372d0d849578</code></li><li>SDK photo <code>ef72c1b658178</code></li></ul>${a('data/catalog.json','ดู Source Manifest','View source manifest')}${tx('Training family ไม่รับรองว่าทุก Example ใช้ได้กับบอร์ดทุก Revision และยังไม่มี Example ที่ยืนยันสำหรับ NDR Base ใหม่ใน PoC นี้','The Training family does not certify every example on every revision. This PoC has no verified examples for the new NDR base.','p')}</section>
<section class="source-section">${tx('แนวคิด Story และงานออกแบบ','Story and design research','h2')}<div class="research-list"><article><h3>Espressif</h3>${tx('เชื่อมภาพ Product, Project ที่ทำได้ และ Developer Journey ให้ผู้ชมมีทางไปต่อ','Connect product capabilities, possible projects and a clear developer journey.','p')}${a('https://esp32-s31.espressif.com/en','ดูเว็บอ้างอิง','View reference')}</article><article><h3>Arduino Project Hub</h3>${tx('เริ่มด้วยสิ่งที่อยากสร้าง แล้วช่วยค้นด้วย Board, Type และ Difficulty','Start with what someone wants to make, then filter by product, type and difficulty.','p')}${a('https://projecthub.arduino.cc/','ดูเว็บอ้างอิง','View reference')}</article><article><h3>NXP App Code Hub</h3>${tx('ทำให้แต่ละ Example มีรายละเอียดที่ช่วยประเมินก่อนเปิด Source','Give examples enough context to evaluate them before opening source code.','p')}${a('https://mcuxpresso.nxp.com/appcodehub','ดูเว็บอ้างอิง','View reference')}</article></div>${tx('การจัด Story และการเลือก Hotspots เป็นข้อเสนอออกแบบของ PoC นี้ ไม่ใช่การรับรองจากเว็บอ้างอิง','The story and hotspot approach are this PoC’s design proposal, not an endorsement by the reference sites.','p')}</section>
<section class="source-section">${tx('ทำไมเลือก Hotspots ก่อน 3D','Why hotspots before 3D','h2')}${tx('ภาพจริงพร้อมหมายเลขช่วยเชื่อมตำแหน่งกับหน้าที่ได้ทันที โหลดเบา และใช้ได้ด้วยเมาส์ สัมผัส หรือคีย์บอร์ด ส่วน 3D เหมาะเป็นมุมมองเสริมเมื่อโมเดลตรงกับ Hardware และจำเป็นต้องดูด้านอื่น','Numbered real-image hotspots connect position to function, keep loading light and work with mouse, touch or keyboard. 3D is a useful secondary view when an accurate model exists and other angles add value.','p')}${tx('เวอร์ชันนี้ยังไม่ได้ฝัง 3D Model เพื่อไม่ให้ภาพ Reference ของ NDR ดูเหมือนเป็นผลิตภัณฑ์ที่เสร็จแล้ว','This version does not embed a 3D model, avoiding the impression that the NDR reference is a finished product.','p')}</section>
<section class="source-section"><h2>Attribution</h2>${tx('TESAIoT เป็นเครื่องหมายของ TESA ใช้เพื่ออธิบายต้นทางของบอร์ดใน Design PoC ไม่ใช่เว็บไซต์ทางการ ภาพอ้างอิง NDR ยังคงสิทธิ์ของเจ้าของเดิม ไม่รวมอยู่ในการให้สิทธิ์โค้ดเว็บแบบ Apache','TESAIoT marks belong to TESA and identify source hardware in this independent design PoC. The NDR reference image retains its original ownership and is excluded from the website code’s Apache license.','p')}${a('https://github.com/nitikorn20/tesaiot-board-explorer-poc/blob/main/THIRD_PARTY_NOTICES.md','อ่าน Asset Notices','Read asset notices')}</section></section>`;
// Retain the verified hardware tour, architecture, simulation and catalog markup.
// The v3 builder selects these sections and supplies the new editorial shell.
export {tx,a,sectionHead,photo,home,examples,sources};
